import React from "react";
import PropTypes from "prop-types";

export default function ArticleAside(props,) {
  return null;
}

ArticleAside.propTypes = {
  contents: PropTypes.object,
};

ArticleAside.defaultProps = {
  contents: {},
};
